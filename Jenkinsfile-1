pipeline {
  agent { label 'stage-slave-amd' }
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'sonarqube-server') { 
          sh './mvnw clean org.sonarsource.scanner.maven:sonar-maven-plugin:sonar'
        }
      }
    }
  }
}
